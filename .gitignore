# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V7
.Trashes
ehthumbs.db
Thumbs.db

# Media uploads — large binary assets, excluded from version control
# Host these via CDN or external storage
wp-content/uploads/

# Node modules (WebGL boilerplate)
node_modules/

# Yarn (not used in this project — use npm)
yarn.lock
.yarn/

# Environment
.env
.env.local
